# loop-orchestrator — commands.env.schema

`.ai/loop/commands.env` 字段定义（保留 ai-coding-loop 原 5 个变量 + 新增 2 个）。

## 字段表

| 变量 | 类型 | 必填 | 默认 | 来源 | 备注 |
|---|---|---|---|---|---|
| `CMD_FMT` | shell 命令字符串 | 否 | `""` | 原 ai-coding-loop | 6 层门禁第 1 层；空 = SKIPPED |
| `CMD_LINT` | shell 命令字符串 | 否 | `( cd ECC-main && npm run lint ) && ( cd ECC-main && ruff check . )` | 原 ai-coding-loop | 6 层门禁第 2 层；实测：lint 有效，ruff 不在 PATH |
| `CMD_TYPECHECK` | shell 命令字符串 | 否 | `""` | 原 ai-coding-loop | 6 层门禁第 3 层；空 = SKIPPED（mypy 未装） |
| `CMD_TEST` | shell 命令字符串 | 否 | `( cd ECC-main && node tests/run-all.js ) && ( cd ECC-main && pytest -q )` | 原 ai-coding-loop | 6 层门禁第 4 层 |
| `CMD_TEST_FILE` | shell 命令字符串 | 否 | `( cd ECC-main && node %FILE% )` | ai-coding-loop skill 默认 | 单文件测试占位符 `%FILE%` |
| `CMD_PROJECT_GATE` | shell 命令字符串 | 否 | `( cd ECC-main && npm run release:approval-gate )` | 原 ai-coding-loop | 6 层门禁第 6 层（项目自有门禁）；**永不 SKIPPED** |
| `CMD_SIZE_CLASSIFY` | JSON | 否 | `""` | **loop-orchestrator 新增** | 6 层门禁前 size 维度判定值，string enum: `trivial`/`small`/`standard`/`large`/空 |
| `CMD_CAPABILITY_REBUILD` | shell 命令字符串 | 否 | `node loop-orchestrator/scripts/capability-scanner.js` | **loop-orchestrator 新增** | capability cache 失效重扫命令；> 7 天提示时调 |

## 数据格式

commands.env 是 shell 脚本源文件，格式：

```bash
# 注释行
VAR="value with \"escaped quotes\""
VAR2=""  # 空 = SKIPPED
VAR3='literal with $not-var-substituted'  # 单引号原样
```

`source .ai/loop/commands.env` 之后 `eval "$CMD_LINT"` 触发。

## shell escape 注意

- 路径含空格：用双引号包裹整条命令
- 嵌套引号：反斜杠转义 `\"`
- 多命令串联：`&&` 或 `;`（取决于失败时是否短路）
- cd 命令：永远显式写 `( cd <dir> && <cmd> )` 子 shell 包裹，**避免污染父 shell cwd**

## 当前实测值（来自 `.ai/loop/commands.env` 段 1 完成时 context-scout 写的版本）

```bash
CMD_FMT=""
CMD_LINT="( cd ECC-main && npm run lint ) && ( cd ECC-main && ruff check . )"
CMD_TYPECHECK=""
CMD_TEST="( cd ECC-main && npm run test ) && ( cd ECC-main && pytest -q )"
CMD_PROJECT_GATE="npm run release:approval-gate"
```

> 注：本 schema 文档不自动应用这些值；段 2 install.js 在末步会用上述值加上 `CMD_SIZE_CLASSIFY=""` + `CMD_CAPABILITY_REBUILD=...` 写出定稿版 `commands.env`。

## 段 2 行为

- `CMD_SIZE_CLASSIFY`：通过 `state.py set-size` 写入 state.json 后，verify.sh 读 state.json 而不是 commands.env
- `CMD_CAPABILITY_REBUILD`：默认指向 capability-scanner.js；install.js 会检测并默认填这一行
