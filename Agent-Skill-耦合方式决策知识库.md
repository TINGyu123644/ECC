# Agent 与 Skill 耦合方式决策知识库（v1.0）

**版本**：v1.0
**更新日期**：2026-07-25
**适用场景**：ECC 架构下，新增/改造 Agent、Skill 时，决策二者绑定耦合关系的统一标准依据
**核心用途**：统一团队开发规范，杜绝 Skill/Agent 绑定混乱、权责不清、执行逻辑不一致问题

---

## 一、核心概念定义

明确 ECC 体系下 Agent、Skill 的底层定位，是耦合决策的基础。

| 概念 | 核心定义 | 核心权责 |
|---|---|---|
| **Agent（智能体）** | 执行任务的「执行者」，定义角色能力、执行逻辑、输出格式 | 负责干活、判断、输出最终结果，是动态执行主体 |
| **Skill（技能）** | 标准化知识库/操作规程，定义「正确标准、固定流程、硬性规则、行业规范」 | 负责定标准、定流程、定边界，是静态规则载体 |
| **耦合方式** | Agent 与 Skill 之间的绑定强度、调用规则、依赖关系 | 决定任务执行的灵活性、标准化程度、容错范围 |

---

## 二、两种耦合方式核心对比

ECC 仅存在两种合法耦合模式：**强耦合（强制流程）** 和 **弱耦合（参考依赖）**。

| 对比维度 | 强耦合 | 弱耦合 |
|---|---|---|
| **Skill 定位** | 刚性**操作规程**，必须 100% 执行 | 柔性**参考知识库**，按需查阅、灵活适配 |
| **调用方式** | 任务启动 Step 0 **强制优先调用 Skill** | 仅在 Related 章节声明依赖，靠 Agent 自主按需引用 |
| **Agent 自由度** | 极低，严格按 Skill 流程执行，禁止跳步、改序 | 高，可根据场景灵活判断、适配、优化执行逻辑 |
| **任务类型** | 纯**执行型**任务 | 纯**判断型 / 评审型 / 咨询型**任务 |
| **二者关系** | 严格执行关系：Agent 执行 Skill 定义的流程 | 松散依赖关系：Agent 参考 Skill 定义的标准 |
| **输出一致性** | 极高，每次执行流程、格式、结果标准完全统一 | 灵活适配，根据场景差异化输出 |

---

## 三、耦合方式决策流程图

新增/改造 Agent-Skill 组合时，严格按此流程判定，无需主观臆断。

```
开始：新增/改造 Agent → 判定任务属性
├─ 任务为「执行型（落地做事、产出固定结果）」
│  ├─ 步骤 ≥5 步 + 执行顺序固定 → 强耦合
│  └─ 步骤少、流程灵活 → 默认弱耦合
└─ 任务为「判断型/评审型（好坏判定、优劣分析）」
   ├─ 需灵活场景适配 → 弱耦合
   └─ 有强制全量校验规则、不可遗漏 → 强耦合
```

---

## 四、精准决策判断表（硬性标准）

### 4.1 强制选择「强耦合」（满足任意 2 条及以上）

> 核心特征：**流程固化、容错极低、遗漏必出错、要求标准化**。

| 序号 | 判断条件 | 场景说明 |
|---|---|---|
| 1 | 流程步骤 ≥5 步，且执行顺序严格固定 | 不允许跳步、乱序、简化流程 |
| 2 | 遗漏任意一步，会导致结果错误、失效、不完整 | 步骤存在强依赖，无替代方案 |
| 3 | 要求每次输出格式、结构、结论完全一致 | 标准化报告、结构化输出、固定模板交付 |
| 4 | 存在不可突破的硬性规则、限制、数据源约束 | 接口延迟、重试机制、固定数据源、权限校验等 |
| 5 | 用户期望一键自动化、黑盒执行，无自定义空间 | 全自动工具类能力，无需人工干预判断 |

### 4.2 强制选择「弱耦合」（满足任意 1 条即可）

> 核心特征：**场景多变、依赖经验、无统一固定流程、需要灵活判断**。

| 序号 | 判断条件 | 场景说明 |
|---|---|---|
| 1 | 需要 Agent 自主判断「该不该做、怎么做更好」 | 输入场景差异大，无法套用固定流程 |
| 2 | 无绝对标准答案，依赖经验、场景、行业规范 | 评审、选型、分析、优化类任务 |
| 3 | 核心价值是灵活性、场景适配、智能决策 | 固化流程会导致能力僵化、适配性差 |
| 4 | Skill 为参考资料、规范准则，非强制执行步骤清单 | 侧重原则、模式、禁忌，无固定执行链路 |

---

## 五、标准化实操模板（可直接复制使用）

### 5.1 强耦合 - Agent 侧固定写法

> 核心要求：**任务启动必先加载 Skill，所有执行逻辑对齐 Skill 规范，禁止重复定义规则**。

```markdown
## Core Workflow

### Step 0 — Always invoke the skill first
Before doing any work yourself, **invoke `Skill('<skill-name>')`** via the Skill tool. This loads the canonical procedure. Subsequent steps reference the skill; never duplicate the skill's detailed rules here.

Skill("<skill-name>")

### Step 1 — [自定义业务第一步]
### Step 2 — [自定义业务第二步]
### Step 3 — [后续执行步骤，严格遵循 Skill 流程]
```

### 5.2 弱耦合 - Agent 侧固定写法

> 核心要求：**仅声明依赖，不强制调用，Agent 自主按需参考**。

```markdown
## Related Skills

- `<skill-name>`: 仅供参考与按需引用，禁止作为强制流程使用

## Behavior

- Agent 自主判断是否需要调用上述 Skill
- 每次执行时根据场景灵活决定调用时机
- 不强制任务流程与 Skill 步骤一一对应
```

### 5.3 Skill 侧固定写法

**强耦合 Skill 在 frontmatter 加 `required: true`**：

```yaml
---
name: <skill-name>
description: <description>
metadata:
  required: true
---
```

**弱耦合 Skill 在 frontmatter 加 `optional: true`**：

```yaml
---
name: <skill-name>
description: <description>
metadata:
  optional: true
---
```

### 5.4 skill-mappings.json 固定写法

**强耦合 - 1 对 1**：
```json
{
  "<agent-name>": ["<skill-name>"]
}
```

**弱耦合 - 1 对 N**：
```json
{
  "<agent-name>": ["<skill-1>", "<skill-2>", "<skill-3>"]
}
```

---

## 六、典型场景对照示例

| 场景 | 任务属性 | 判定结果 | 实际案例 |
|---|---|---|---|
| 每日自动拉取股票数据 + 生成报告 | 执行型 | **强耦合** | `stock-analyzer` ↔ `stock-analyst` |
| 宏观数据监控 + 每日推送 | 执行型（固定流程） | **强耦合** | `macro-monitor` |
| 智能 PPT 生成（按用户输入动态适配） | 判断型（场景多变） | **弱耦合** | `ppt-generator` |
| 代码审查 | 评审型（依赖经验） | **弱耦合** | `coding-standards` ↔ `code-reviewer` |
| 自省式捕获会话学习 | 判断型（场景多） | **弱耦合** | `self-improvement` ↔ `self-improver` |

---

## 七、违反决策的代价

| 错误 | 后果 |
|---|---|
| 弱耦合用强耦合写法 | 失去灵活性，Agent 僵化无法适配场景变化 |
| 强耦合用弱耦合写法 | 流程不可控，执行结果不一致，用户无法依赖 |
| 1 对 N 写成 1 对 1 | Agent 只能用 1 个 skill，能力受限 |
| 1 对 1 写成 1 对 N | Skill 失去主从关系，Agent 不知道该优先调谁 |
| 描述不标 skill 源 | 后续维护者不知道该 agent 是干什么的，重构困难 |

---

## 八、决策检查清单

新增/改造 Agent-Skill 组合时，对照检查：

- [ ] 任务类型已判定（执行型 / 判断型 / 评审型 / 咨询型）
- [ ] 满足 §4.1 强耦合条件中至少 2 条 → 强耦合
- [ ] 满足 §4.2 弱耦合条件中至少 1 条 → 弱耦合
- [ ] Agent 描述符合 §5.1 或 §5.2 模板
- [ ] Skill frontmatter 含 `required: true` 或 `optional: true`
- [ ] `skill-mappings.json` 1 对 1 或 1 对 N 写法正确
- [ ] 已记录到 PR/CHANGELOG（让团队知道为什么这么判定）

---

**最终声明**：本知识库是 ECC 体系下 Agent-Skill 耦合判定的**唯一权威标准**，所有 PR 涉及新增/改造 Agent 或 Skill 时必须遵守。